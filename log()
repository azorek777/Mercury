#Funkcja sprawdza hasło i login użytkownika, ustawia który to użytkownik w zmiennej str user
#w pliku zapis wygląda tak:
'''
(login)
(hasło)
(identyfikator użytkownika)
(login 2)
(hasło 2)
(identyfikator użytkownika 2)
...
'''
def log():
    plik = open("logins and passwords.txt", "r").read()
    lines = plik.split("\n")
    login = str(input("Podaj login: "))
    password = str(input("Podaj haslo: "))
    user = 0
    for i in range(len(lines)):
        if (login == lines[i]):
            i += 1
            if(password == lines[i]):
                i+=1
                print("zalogowano na", lines[i])
                user = "student"
    if (user == 0):
        print("nieprawidłowy login lub hasło")
        log()
